# Мислене за рекурсията по-просто

<img width="150" height="146" alt="fx" src="https://github.com/user-attachments/assets/ef8e8eef-7a8a-4979-aef9-32881c38eae6" />


**Рекурсия = функция, която се повтаря сама, докато стигне краен случай.**

Представете си го като сграда с етажи:

Трябва да се спуснете до най-долния етаж (базов случай).
На всеки етаж правите малка задача и после извиквате същата процедура за следващия етаж.
Когато стигнете долу, всички задачи се решават по пътя нагоре.

**Пример на аналогия без програмиране:**

Искате да събереш 10 кутии от тавана.
Вземате първата кутия. Ако има още кутии → повтаряте процеса за останалите.
Когато няма повече кутии → спирате.

**Рекурсията е просто мисловен модел за повтаряне, докато не свърши нещо, без да мислите за “цикъл” или “променлива”.**

🔹 Малък Haskell пример (*буквално като игра*)

Да съберем числата от 1 до N:

haskell<br>sumUp 0 = 0<br>sumUp n = n + sumUp (n-1)<br>

Как да го разберем без да знаем цикли:

*sumUp 3 → 3 + sumUp(2)*

*sumUp(2) → 2 + sumUp(1)*

*sumUp(1) → 1 + sumUp(0)*

*sumUp(0) → 0 (краен случай)*

После започваме да връщаме резултатите:

1 + 0 = 1
2 + 1 = 3
3 + 3 = 6 ✅

Тоест рекурсията е като “разгъване и сгъване” на задачи, вместо да мислиш за цикъл.

🔹 **Малка техника за упражнение**

Изберете малка задача: примерно „броя от 1 до 5“.

Нарисувайте си стълба или списък на лист хартия.

Отдолу нагоре пишете *“краен случай = 5”* и после мислите как ще се върнете обратно към началото.

След това опитайте да го напишете на **Haskell** като рекурсивна функция.

💡 Съвет:
Не се опитвайте да измисляте големи програми веднага. Мислете за малки, еднолинейни функции, дори само да броят, сумират или връщат списък. Рекурсията е по-лесна, когато първо “виждате” задачата като серия от малки стъпки, не като “цялата програма наведнъж”.


**Идеята: рекурсия = разгъване и сгъване**

Когато казваме *„когато стигнем долу, всички задачи се решават по пътя нагоре“*, всъщност описваме две фази на рекурсията:

**Разгъване (going down)**

Функцията се вика сама и „пуска“ задачите една по една, докато не стигне базовия случай.
По време на разгъването нищо реално не се “събира” — просто си мислим какво трябва да се случи.

Пример: sumUp 3 → 3 + sumUp(2) → 2 + sumUp(1) → 1 + sumUp(0)

**Сгъване (coming back up)**

Когато достигнем базовия случай (например sumUp 0 = 0), рекурсията започва да връща стойности обратно.

Всяка функция, която е “изчаквала” резултата от следващата стъпка, сега може да го използва.

Пример:

sumUp(0) връща 0

sumUp(1) връща 1 + 0 = 1

sumUp(2) връща 2 + 1 = 3

sumUp(3) връща 3 + 3 = 6 ✅

Тоест “по пътя нагоре” означава: всяка функция чака резултата от функцията, която е извикала по-долу, и после добавя/обработва своето.

🔹 Кой го прави?

Никой човек не го прави ръчно. 😏

Компилаторът на Haskell и стека на функцията го правят автоматично.

Всеки път, когато извикаш функция рекурсивно, компютърът запазва къде е трябвало да продължи след като получи резултата.

Когато достигнеш базовия случай, компютърът започва да “връща тези резултати обратно” по стека.
